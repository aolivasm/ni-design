% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-size.R
\name{ni_sample_size}
\alias{ni_sample_size}
\title{Convert a target effect-estimator variance to sample size}
\usage{
ni_sample_size(
  required.variance,
  effect.measure = c("relative_risk", "risk_difference", "mean_difference"),
  allocation.ratio = 1,
  dropout = 0,
  risk.experimental = NULL,
  risk.control = NULL,
  sd.experimental = NULL,
  sd.control = NULL
)
}
\arguments{
\item{required.variance}{Maximum variance allowed for the estimated
experimental-versus-control effect.}

\item{effect.measure}{One of \code{"relative_risk"}, \code{"risk_difference"}, or
\code{"mean_difference"}.}

\item{allocation.ratio}{Experimental-to-control allocation ratio.}

\item{dropout}{Expected dropout proportion. Supply one value for both arms
or two values ordered experimental, control.}

\item{risk.experimental, risk.control}{Event risks at the analysis time;
required for binary effect measures.}

\item{sd.experimental, sd.control}{Outcome standard deviations; required for
a mean difference.}
}
\value{
A one-row data frame with analyzable and randomized sample sizes and
the variance achieved by the analyzable sample.
}
\description{
Computes two-arm sample sizes using large-sample variances for the log
relative risk, risk difference, or mean difference. The experimental-to-
control allocation ratio is \code{n_experimental / n_control}.
}
\details{
The formulas are
\deqn{Var(log(RR)) = (1-p_X)/(n_X p_X) + (1-p_C)/(n_C p_C),}
\deqn{Var(RD) = p_X(1-p_X)/n_X + p_C(1-p_C)/n_C,}
and
\deqn{Var(MD) = sigma_X^2/n_X + sigma_C^2/n_C.}
Dropout inflates randomized counts and is assumed non-informative; it does
not alter the analyzable-sample variance formula.
}
\examples{
ni_sample_size(
  required.variance = 0.01,
  effect.measure = "relative_risk",
  risk.experimental = 0.10,
  risk.control = 0.15
)
ni_sample_size(
  required.variance = 0.04,
  effect.measure = "mean_difference",
  sd.experimental = 2,
  sd.control = 2
)
}
