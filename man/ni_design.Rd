% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design-survival.R
\name{ni_design}
\alias{ni_design}
\title{Design a non-inferiority trial on the prevention-efficacy scale}
\usage{
ni_design(
  u.list = c(1, 0, 0, 0),
  l1.list = c(0, 0, -0.25, -0.25),
  f.preserv = 0.5,
  null.pe = 0.3,
  design.alternative.pe = 0.85,
  hist.ac.pe = 0.55,
  hist.ac.effect.se = 0.25,
  lambda0.for.design = 0,
  target.on.unconditional.power = TRUE,
  allocation.ratio = 1,
  power = 0.9,
  sign.level = 0.025,
  lambda0.sens.analysis = NULL,
  placebo.incidence.rate = 0.03,
  loss.to.followup = 0.075,
  trial.duration = 2,
  correction = FALSE
)
}
\arguments{
\item{u.list, l1.list}{Equal-length vectors of framework parameters.}

\item{f.preserv}{Fraction of the active-control effect to preserve, or
\code{NULL} to omit this criterion.}

\item{null.pe}{Minimum prevention efficacy for the inferred-efficacy
criterion, or \code{NULL} to omit it.}

\item{design.alternative.pe}{Experimental prevention efficacy under the
design alternative.}

\item{hist.ac.pe}{Historical active-control prevention efficacy.}

\item{hist.ac.effect.se}{Standard error of the historical effect estimate
on the log hazard-ratio scale.}

\item{lambda0.for.design}{True relative effect deviation assumed at design.}

\item{target.on.unconditional.power}{If \code{TRUE}, target unconditional power;
otherwise target conditional power.}

\item{allocation.ratio}{Experimental-to-control allocation ratio.}

\item{power}{Desired power.}

\item{sign.level}{One-sided significance level.}

\item{lambda0.sens.analysis}{Optional relative effect deviation for a power
sensitivity analysis.}

\item{placebo.incidence.rate}{Annual placebo incidence.}

\item{loss.to.followup}{Annual loss-to-follow-up proportion.}

\item{trial.duration}{Fixed follow-up duration in years.}

\item{correction}{Apply the manuscript's one-event-per-arm adjustment for
interim monitoring.}
}
\value{
An object of class \code{ni.design} containing specifications and one
data frame per requested success criterion.
}
\description{
This is the primary function described in Appendix B of
Olivas-Martinez, Gao, and Janes (2026). It preserves the manuscript's
argument names and output structure.
}
\examples{
design <- ni_design(
  u.list = c(1, 0),
  l1.list = c(0, 1.96 * 0.61 / log(1 - 0.928)),
  design.alternative.pe = 0.95,
  hist.ac.pe = 0.928,
  hist.ac.effect.se = 0.61
)
summary(design)
}
